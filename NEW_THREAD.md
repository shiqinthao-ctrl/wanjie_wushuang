# NEW_THREAD.md

When Codex conversation becomes large or starts compacting repeatedly:

1. Ask the current Codex run to update `handoff/STATE.md`.
2. Start a new thread in the same project.
3. Send only:

> Read `AGENTS.md`, `handoff/STATE.md`, and `TASK.md`. Continue the current task. Do not preload other docs; use `docs/INDEX.md` only when required.

Do NOT paste:
- previous chat transcript
- full PRODUCT_SPEC
- full ROADMAP
- full source files

The repository is the source of truth.
