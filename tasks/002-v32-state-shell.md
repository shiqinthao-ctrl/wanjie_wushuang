# tasks/002-v32-state-shell.md

Use only after TASK.md Phase 1A is complete.

Goal: introduce `WW.state` shell and compatibility accessors without migrating combat arrays.

Read: `handoff/STATE.md`, `docs/GLOBAL_RUNTIME_AUDIT.md`, relevant core/save files only.

Do not migrate `enemies`, `shots`, or rewrite `updateRun`.

Done when existing behavior remains unchanged and all repository checks pass.
