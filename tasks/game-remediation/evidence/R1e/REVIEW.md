# R1e 范围与代码复核

复核对象：`8505a3d` 之后的表现映射、章节声音、设置及验证代码。
这是实施者的范围复核，不冒充外部审查或真人试玩。

## 已核对

- 核心只增加确认后的表现事件；攻击和受伤仍在核心结算，动画/声音不回写伤害。
  `feedback()` 在 G5 无操作；新升级提示只在 chapter 下发出，未修改经验与候选规则。
- Phaser 保留唯一更新入口。音频时钟只确定节点时长/混音，不推进 GameCore。
  固定种子音频开关测试比较 UI 快照、全部渲染实体和升级候选，保持相同结果。
- 继续战斗的事件屏障保留画面事件但丢弃声音积压；新进化事件仍可在第一帧播放。
  窗口 blur 采用非捕获监听，避免升级卡片焦点切换错误停止声音。
- 声音最大 6 声部、同类限频、危险音优先；异步解锁拒绝不冒泡阻断战斗，允许下次重试。
  destroy 幂等；真实浏览器连续 20 次关闭上下文并清空活动振荡器。
- 只新增隔离的声音偏好键；设置写失败提示且当前选择有效，存档 Schema30 未更改。
  无新增依赖、外部服务、凭证、动态执行入口或公共调试 UI。
- 新测试夹具和音频观测仅在 Playwright 注入；构建哈希脚本扫描公共产物中的夹具标识。
  旧有测试断言保留，失败尝试及原片保留。
- 人工查看 360/390/430 宽度的截图：声音开关、音量及错误提示完整，无横向溢出。
  暂停页内容仍长，继续按钮需要滚动；浏览器流程验证可操作，R1f 应改善信息密度。

## 验证中修复的问题

初次浏览器测试定位到错误的赤龙技能按钮定位、恢复第一帧丢弃进化声音、
升级按钮失焦被捕获为窗口后台等问题。已修正定位器、显式恢复屏障与窗口监听，
并新增连续升级/觉醒、隐藏后手动继续的回归。详见 RESULTS.md 的各次尝试。

## 保留的局限

工程合成音色不是成品音效。专业图集/动画未到位；音频不可用时采用可游玩的静音降级，
没有新增设备能力警报。Chrome 自动化证明实际信号和资源释放，未覆盖 iOS Safari 系统
中断、蓝牙设备切换、真机扬声器听感或长时间发热。现有大包提示仍存在。
完整自然首领链、生命耗尽、冷启动历史问题及真人/真机/PWA 验收继续保留。
